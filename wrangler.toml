name = "voiceover-guide"
pages_build_output_dir = "site/public"

# Note: The build command (`bash ./build.sh`) must be set in the Cloudflare Dashboard
# Settings -> Builds & deployments -> Build command

[vars]
# Make sure Hugo 0.122+ or equivalent is used in Cloudflare's environment
HUGO_VERSION = "extended_0.124.1"
NODE_VERSION = "20"
